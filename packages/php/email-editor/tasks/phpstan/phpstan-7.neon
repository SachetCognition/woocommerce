# Use the main phpstan config
includes:
	- phpstan.neon

# Rewrite tested PHP version to minimum supported (8.2)
# str_starts_with/str_ends_with ignore patterns removed: native in PHP 8.0+
parameters:
	phpVersion: 80200
